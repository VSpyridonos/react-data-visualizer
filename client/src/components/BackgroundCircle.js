// shortcut for arrow funcs: rafce
const BackgroundCircle = ({ radius, strokeWidth }) => (
    <circle
        r={radius}
        fill='lightyellow'
        stroke='black'
        strokeWidth={strokeWidth}
    />
);

export default BackgroundCircle


